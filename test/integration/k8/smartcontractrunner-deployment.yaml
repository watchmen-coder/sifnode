apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.22.0 (HEAD)
  creationTimestamp: null
  labels:
    io.kompose.service: smartcontractrunner
  name: smartcontractrunner
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: smartcontractrunner
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert
        kompose.version: 1.22.0 (HEAD)
      creationTimestamp: null
      labels:
        io.kompose.network/sifchaintest: "true"
        io.kompose.service: smartcontractrunner
    spec:
      containers:
        - args:
            - bash
            - -c
            - PATH=${PATH}:/gobin python3 -u src/py/env_framework.py smartcontractrunner
          image: ghcr.io/sifchain/sifnode/sifdocker:latest
          name: smartcontractrunner
          resources: {}
          volumeMounts:
            - mountPath: /sifnode
              name: claim0
            - mountPath: /configs
              name: claim1
            - mountPath: /logs
              name: claim2
            - mountPath: /gobin
              name: claim3
            - mountPath: /gocache
              name: claim4
          workingDir: /sifnode/test/integration
      restartPolicy: Always
      volumes:
        - name: claim0
          persistentVolumeClaim:
            claimName: claim0
        - name: claim1
          persistentVolumeClaim:
            claimName: claim1
        - name: claim2
          persistentVolumeClaim:
            claimName: claim2
        - name: claim3
          persistentVolumeClaim:
            claimName: claim3
        - name: claim4
          persistentVolumeClaim:
            claimName: claim4
status: {}
