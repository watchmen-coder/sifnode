{{- if .Values.persistence.enabled }}
{{- range $key, $value := .Values.pvcs}}
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ $key }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ $value }}
---
{{ end }}
{{- end }}